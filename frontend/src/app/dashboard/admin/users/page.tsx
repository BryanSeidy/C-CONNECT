'use client';
import React from 'react';
export default function AdminUsers() {
  return (
    <div style={{background:'var(--bg-card)',borderRadius:'var(--radius-md)',padding:'2rem',color:'var(--text-muted)'}}>
      Gestion des utilisateurs — À implémenter avec l&apos;endpoint /api/admin/users.
    </div>
  );
}
